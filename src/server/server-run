#!/usr/bin/env python3
import server
import arg_parser
arguments = arg_parser.Arguments('server-run')

ip_addr = arguments.get_arg('host')
port = arguments.get_arg('port')

# init server
server = server.Server(ip_addr, port)
server.run()

